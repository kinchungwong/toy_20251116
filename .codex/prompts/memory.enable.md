# Enable Memory Use by Agents

Agents are given full access to the following directories:

- `agents/memory/records` (persistent, quasi-immutable)
- `agents/memory/long-term` (persistent, mutable)
- `agents/memory/short-term` (ephemeral, mutable, suitable for planning and to-do lists)
